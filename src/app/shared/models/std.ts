export interface Istud {
    fname: string;
    lname: string;
    email: string;
    contact: number;
}

export interface Iemp {
    fname: string;
    lname: string;
    email: string;
    contact: number;
}

export interface Iprod {
    prodName: string;
    prodDescr: string;
    // prodCat: "Product" | "Catalogue"
    prodCat: string
}